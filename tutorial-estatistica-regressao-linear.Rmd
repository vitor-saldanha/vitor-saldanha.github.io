---
title: "Regressão Linear"
description: |
  Uma introdução prática aos modelos lineares no R.
output: 
  distill::distill_article:
    toc: true
---

```{css, echo=FALSE}
/* --- CUSTOMIZAÇÃO AVANÇADA DE LAYOUT DISTILL --- */

/* 1. CONFIGURAÇÃO DO BANNER (HERO IMAGE) */
d-title {
  /* Layout e Posicionamento */
  width: 100vw;                 /* Ocupa 100% da largura da janela */
  position: relative;
  left: 50%;
  right: 50%;
  margin-left: -50vw;           /* Técnica para centralizar elemento full-width */
  margin-right: -50vw;
  margin-top: 0;
  margin-bottom: 3rem;

  /* Estilo do Fundo */
  /* NOTA: Substitua a URL pela imagem do seu projeto se desejar */
  background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), 
                    url('https://images.unsplash.com/photo-1555066931-4365d14bab8c?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80)');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;

  /* Espaçamento */
  padding-top: 6rem;
  padding-bottom: 6rem;

  /* Tipografia */
  text-align: center;
  color: white;
  
  clip-path: inset(0 0 1px 0);

}

/* Título */
d-title h1 {
  color: #ffffff !important;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  font-size: 3.5rem;
  font-weight: 700;
  text-shadow: 0 2px 4px rgba(0,0,0,0.5);
  margin-bottom: 1rem;
}

/* Descrição */
d-title p {
  color: #f0f0f0 !important;
  font-size: 1.4rem;
  font-weight: 300;
  max-width: 800px;
  margin: 0 auto;
}

/* Metadados (Autores/Data) */
d-byline {
  border-top: none !important;
  margin-top: 2rem;
}

d-byline .byline {
  color: rgba(255, 255, 255, 0.8) !important;
}

d-byline a {
  color: #fff !important;
  text-decoration: underline;
}

/* 2. CONFIGURAÇÃO DO MENU LATERAL (TOC) */
@media (min-width: 1000px) {
  /* Aplica apenas em telas largas onde o TOC lateral existe */
  
  .d-contents nav {
    /* Comportamento Sticky */
    position: -webkit-sticky;
    position: sticky;
    top: 100px; /* Ajuste para não colidir com a Navbar */
    
    /* Alinhamento Vertical */
    align-self: start;
    margin-top: 0 !important;

    /* Scroll interno se necessário */
    max-height: 80vh;
    overflow-y: auto;
  }

  /* Ajuste do container pai */
  .d-contents {
    grid-row: span 10; /* Permite que o container acompanhe o texto longo */
    padding-top: 0 !important;
  }

  /* Estilo dos links do TOC */
  .d-contents nav a {
    color: #666;
    border-left: 2px solid #ddd;
    padding-left: 10px;
  }

  .d-contents nav a:hover {
    color: #2c3e50;
    border-left: 3px solid #2c3e50;
  }
}
```


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

<div class="rec-section">

<div class="rec-title">
<i class="fas fa-heart" style="color: #ff8fa3;"></i> 
Gostou? Veja também:
</div>

<div class="rec-grid">
<a href="tutorial-testet.html" class="rec-card">
<i class="fas fa-chart-bar rec-icon"></i>
<span class="rec-text">Teste T de Student</span>
<span class="rec-sub">Comparação de médias</span>
</a>

<a href="tutorial-quiquadrado.html" class="rec-card">
<i class="fas fa-table rec-icon"></i>
<span class="rec-text">Qui-Quadrado</span>
<span class="rec-sub">Dados categóricos</span>
</a>

<a href="metanalise.html" class="rec-card">
<i class="fas fa-layer-group rec-icon"></i>
<span class="rec-text">Metanálise</span>
<span class="rec-sub">Introdução</span>
</a>
</div>

<div class="rec-footer">
<a href="estatistica.html" class="rec-btn-back">
<i class="fas fa-arrow-left"></i> Voltar aos Tutoriais
</a>
</div>

</div>

<div class="author-footer">

<span class="author-name">Escrito por Vítor Saldanha</span>

<div class="author-bio">
Estudante de medicina com foco em pesquisa acadêmica, estatística e medicina baseada em evidências.
</div>

<div class="post-meta-date">
<i class="far fa-calendar-alt"></i> 
Atualizado em: 28 de Dezembro de 2025
</div>

</div>
